# Browser Google Drive picker (safe to expose; restrict by HTTP referrer / JS origin).
VITE_GOOGLE_CLIENT_ID=
VITE_GOOGLE_API_KEY=
# Optional. Defaults to the numeric prefix of the client ID.
VITE_GOOGLE_APP_ID=
